- `Array.prototype.with()`: returns a new array with the element at the given index replaced, without modifying the original array.

## npm

- Fake REST AP: [json-server](https://www.npmjs.com/package/json-server/v/0.17.4)